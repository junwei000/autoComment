{
  "name": "autocomment-extension",
  "version": "2.0.0",
  "private": true,
  "description": "Chrome extension that batch-generates blog comments with your own OpenRouter key",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
